---
name: skill-name
description: Runs named workflow on explicit user request.
metadata:
  invocation: user
---

# Skill title

Use this template only for clients supporting explicit user-only invocation metadata. Add client-specific invocation control during installation; Agent Skills core specification does not define it.

1. Perform requested workflow.
2. Validate observable result.
